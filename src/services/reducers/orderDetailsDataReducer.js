const initialState = {
    orderNumber: null,
    isShowing: false,
    isLoading: false,
    hasError: false,
}

export const orderDetailsDataReducer = ( state = initialState, action) => {
    switch(action.type) {
        
    }
}